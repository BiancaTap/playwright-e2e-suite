# automationexercise.com is a public demo site; no real secrets needed.
BASE_URL=https://automationexercise.com

# Seed for deterministic faker data. Override to reproduce a specific flake.
FAKER_SEED=

# Optional: persist test reports to a non-default folder
# REPORTS_DIR=reports
